# Boot Architecture

Default:
- No Button -> EmulationStation
- L1 -> Maintenance Mode
- R1 -> XFCE Desktop
- R2 -> Runtime 3 (placeholder)
- SELECT -> PineRE Boot Manager

The Boot Manager is stateless and exists only to let the user choose what to boot.
